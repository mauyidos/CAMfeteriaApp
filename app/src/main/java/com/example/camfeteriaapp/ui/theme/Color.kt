package com.example.camfeteriaapp.ui.theme

import androidx.compose.ui.graphics.Color

// Gradiente principal
val PrimaryStart = Color(0xFF1A2A44)
val PrimaryEnd = Color(0xFF2F5D8C)

// Secundario
val Secondary = Color(0xFF4DA8DA)

// Café (identidad)
val Coffee = Color(0xFFA47148)
val CoffeeLight = Color(0xFFD9A066)
val CoffeeDark = Color(0xFF6F4E37)

// Fondo claro
val Background = Color(0xFFF5F3EF)
val BackgroundGradient = Color(0xFFE7E3DC)

// Texto
val TextPrimary = Color(0xFF2D2A26)
val TextSecondary = Color(0xFF8A817C)

// Error
val ErrorColor = Color(0xFFD64545)